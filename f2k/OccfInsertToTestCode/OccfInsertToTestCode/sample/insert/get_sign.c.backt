/*
 * First KLEE tutorial: testing a small function
 */

int get_sign(int x) {
	int main;
  if (x == 0)
     return 0;
  
  if (x < 0)
     return -1;
  else 
     return 1;
}

int mainsub(){
	return 1;
}

int main() {
  int a;
  int main;
  klee_make_symbolic(&a, sizeof(a), "a");

  return get_sign(a);
} 

int Main(){
	return 1;
}